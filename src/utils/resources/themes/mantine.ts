import { ColorScheme, MantineThemeOverride } from '@mantine/core';

export const THEME_MANTINE = (colorScheme: ColorScheme): MantineThemeOverride => ({
  /** Put your mantine theme override here */
  colorScheme,
  // fontFamily: 'Inter, sans-serif',
});
